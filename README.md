# Nate VST

Mac-first VST3 instrument plugin for Ableton Live, focused on house, tech house, techno, minimal, and related electronic production.

The long-term direction is a hybrid synth with subtractive synthesis, wavetable-style movement, sampling/slicing, random sound generation, internal pattern tools, and an effects area.

## Current Target

- macOS first.
- Ableton Live as the main host.
- VST3 first.
- JUCE + CMake.
- C++20.

## Product Direction

The first useful version should load in Ableton, respond to MIDI, generate sound, expose automatable parameters, and save state in a Live Set.

Planned major areas:

- Synth engine: oscillator 1, oscillator 2, sub, noise colors with percussive decay, internal wavetable mode with Osc 1/Osc 2 WT position controls, appended procedural Organ and House Piano source modes for house stabs, a live/clickable SYNTH house layer rack that reads existing sources as Sub/Body/Character/Transient/Chop roles with an expanded source-layer focus overlay, Osc Warp source shaping, unison/spread, envelopes, multimode filter with a live response display and modulation range overlay, Filter Character and Slope modes, drive, editable modulation with per-route bypass/delete, activity-metered LFO 1/LFO 2/Mod Env/Velocity/S&H/Smooth/Chaos plus Mod Wheel/Aftertouch/Pitch Bend/Note expression sources, and eight stable performance macros for club-ready tone, dirt, motion, space, weight, bounce, warp, and throw moves. Saw, square, triangle, wavetable, organ, and house-piano sources include a first bandlimited/procedural quality pass for cleaner bright stabs and bass harmonics.
- Sound Lab: page-based guided randomization with focused Generate, Mutate, Recipe, History, and Save areas, recipe intent/tempo/use-case summaries, Vary/Mutate/Wild mutation strengths, per-section Source/Env/Filter/Sample/FX/Seq/Macro generation strength, one-click section rolls, four recallable/generated-cue candidate slots with visible compare/diff detail, changed-section diff badges, save-from-slot with favorite/rating handoff, and A/B snapshot promotion, recipes, section locks, Random Lab undo/redo, useful-only validation with short render checks, auto-retry, and recipe-aware safe fallbacks for audible/source-safe/output-safe generated patches, generated preset draft naming, editable generated preset metadata notes with reusable note templates, category/pack/BPM save suggestions, and safety rules.
- Sample Lab: load audio, view the waveform with chop modulation range feedback, detect transient starts into eight custom slice regions, drag existing slice boundaries directly in the waveform lane, jump and audition phrase slices with eight pads, see selected/custom slice overlays with pitch/pan/chance/reverse/stutter/choke/nudge/fade badges, open a larger adaptive chop focus overlay, drag chop windows with visible handles, reverse/pitch/gain/mix it, choose Classic/Granular/Spectral/Cloud sample engine modes with Grain/Spray/Freeze controls, store per-slice region/pitch/gain/pan/chance/reverse/stutter/choke/nudge/fade behavior, and trigger Slice Keys from MIDI C3-G3.
- Sequencer: internal 16-step piano-roll style pattern area with root-aware note rows, scale helper, chord/stab modes including Maj 7, Dom 7, Sus, House 9, and Dub, voicing and strum controls, beat grouping, editable velocity/probability/timing/length/lock/ratchet/condition/slide lanes, assignable lock destinations for cutoff, drive, Osc Warp, pump, delay, reverb, and wavetable position movement, appended groove modes for house shuffle, tech-house, minimal skip, and techno drive, transport/PPQ status, four pattern scenes for A/B/Fill/Drop variations, first-pass live scene-chain step/control playback, an adaptive expanded SEQ focus `Auto` / `2 Bar` / `4 Bar` chain length mode, a `Build 4-Bar Chain` transform for generating A/B/Fill/Drop phrase variations, rotate, variation, undo, click-to-save and drag-to-Ableton MIDI export for the current pattern plus captured A/B/Fill/Drop scene-chain export with groove/ratchet/condition/slide timing, and quick pattern tools for deep-house chords, dub chords, chord-stab painting, offbeat bass, rolling bass, bass-contour shaping, tech-house basslines, techno pulses, minimal plucks, UKG riffs, and generated ideas.
- FX: reorderable post-synth/post-sample rack with a fixed final Guard safety stage, host-sync status, send-style delay/reverb throws, one-click and hold-to-restore delay/space/pump throws, mute drops, tone cleanup, three-band EQ, bass-safe Drive saturation, pump with live phase/reduction telemetry, tremolo/auto pan, ring modulation, comb resonance, phaser, flanger, chorus, delay, reverb, width, output trim, final-output oscilloscope plus spectrum snapshot telemetry with held peaks and grouped club-band balance, and Guard safety clipping with glue compression, transient punch, clip blend, live output safety, and reduction status.
- Library: save with a visual target trail, metadata readiness preview, same-folder overwrite confirmation, and expanded style folder/pack targets, load, rendered preset preview cache/playback with preview-ready/stale row status, visible-row preview warming, preview level badges, compact-width rows that preserve sound name/metadata while collapsing dense macro strips, role-aware audition render phrases, compare/revert recent preset loads, search, sort, smart crates, tag, favorite, rate, a left crate rail with user/factory/folder/pack/style coverage, an adaptive selected-preset profile card with role/traits/cue/profile meters, macro-value preview, preset notes, and metadata-label categorized `.natevstpreset` files from recursive user and factory preset folders, including House, Tech House, Techno, Minimal, UKG, pack, key, and tempo filters.

## Interface

The editor uses focused panels instead of one tall stacked page:

- `HOME`
- `SYNTH`
- `LAB`
- `MOD`
- `SAMPLE`
- `SEQ`
- `FX`
- `LIBRARY`
- `INFO`

The `HOME` panel is the default dashboard for quick patch shaping, source balance, core shape controls, club low-end guidance, a central live overview for source/macro motion, cutoff/drive/pump/send/output status, a compact `SOURCE -> FILTER -> MOTION -> FX -> GUARD -> OUT` signal-flow strip, final-output oscilloscope shape, spectrum energy with held peaks and sub/low/mid/presence/air balance, stereo field/correlation, a Club Monitor for short-history sub, low-side, pump, Guard, and peak risk, an editable eight-macro shape map with an expanded focus overlay, one-gesture Motion/Space XY movement, compact randomization, A/B/C/D performance scenes, and a Patch Snapshot view for patch role, source type, output safety, scene readiness, sequencer/pump state, random candidate context, preset recall, auditioning, and six compact Tone/Dirt/Move/Space/Weight/Bounce performance meters. The `SYNTH` panel exposes Sine, Saw, Square, Triangle, WT, Organ, and Piano source selectors, a compact house layer rack for Sub/Body/Character/Transient/Chop source roles, voice controls, Osc Warp, filter mode, Filter Character, Filter Slope, Osc 1/Osc 2 WT position controls, a labeled 3D wavetable/custom-wave surface with a top morph-frame rail, Osc 1/Osc 2 position pins, draw modes, spectral tilt heatmap, single-cycle WAV import/export, and editable partial bars, and a live filter response display with modulation range feedback for deeper patch edits without crowding HOME. The `LAB` panel is the full randomization workspace, split into focused `Generate`, `Mutate`, `Recipe`, `History`, and `Save` pages for recipe selection with genre/tempo/use-case summaries, scoped generation, per-section generation strength, useful-only validation feedback from parameter and short-render checks, lock feedback, section-roll buttons, four generated-candidate recall slots, non-destructive candidate `Cue` buttons, visible History detail for active candidate sections/traits/diffs/validation, changed-section count badges, `Save Slot`, candidate save favorite/rating controls, `To A`/`To B` snapshot promotion, mutation controls, and editable generated-preset save metadata notes plus reusable macro/use-case templates without showing every control at once. The `MOD` panel includes LFO 1 with an editable eight-point curve tool, LFO 2, Mod Env 1, activity-metered modulation source rows for LFO, envelope, velocity, macros, random sources, Mod Wheel, Aftertouch, Pitch Bend, and Note position, a visual macro shape map, a draggable macro-assignment pad backed by the saved matrix slots, an expanded macro focus overlay for larger assignment editing, right-click control assignment, and an editable eight-slot matrix with per-route bypass/delete for safe synth, sample, and FX destinations, including send delay/reverb amounts. Waveform, oscillator 2 waveform, filter mode, sample engine, and sequencer-rate controls use compact selectors. The `SAMPLE` panel includes a clearer recorder flow with Record, Commit, Play, Trim, Splice, and Mangle actions, a waveform overview with Sample Start/Mix/Pitch/Ramp/Stutter modulation feedback, draggable start/end region, phrase markers, selected/custom slice overlays with performance badges, direct slice-boundary dragging, slice-lane click selection, eight slice pads, Classic/Granular/Spectral/Cloud engine modes with Grain/Spray/Freeze controls, and an adaptive expanded chop focus overlay for larger vocal-chop editing on tall host windows. The `SEQ` panel includes one-tap Bass, Stab, and UKG pattern shortcuts plus house, UKG, tech-house, minimal, and techno groove templates with expanded chord colors, voicing, strum, variation, undo, rotate, click-to-save and drag-to-Ableton single-pattern MIDI export, captured scene-chain MIDI export/drag-out, a first-pass `Live` toggle for playing captured scene steps and controls as a live chain, an expanded-focus `Auto` / `2 Bar` / `4 Bar` chain length control for live/export/drag behavior, per-step Lock, Rat, Cond, and Slide lanes, A/B/Fill/Drop pattern scene capture/recall, an adaptive expanded pattern focus overlay for larger piano-roll and lane editing, and a host-sync badge for checking Ableton transport lock while building club patterns. The `FX` panel includes one-click Delay Throw, Space Throw, Pump Drop, and Dry Reset actions plus hold-to-restore delay, space, pump, and mute-drop buttons above the reorderable rack; Delay and Reverb detail views include separate Send knobs so throw tails can be distinct from serial insert mix, and Guard exposes Push, Glue, Punch, Clip, and Ceiling for club-safe dynamics. The Pump display now shows HOST/INT timing, live phase, and ducking reduction as the first sidechain-visualization foundation, while HOME signal flow/scope/spectrum/stereo field/Club Monitor, the Guard slot, and top-bar output meter report active path, live waveform, held spectrum energy, grouped club-band balance, width/correlation, short-history club risk, LOW/SAFE/HOT/CLIP level status, and active Guard reduction. A compact top-bar output meter stays visible across panels, a persistent selected-control strip includes first-pass global `Undo Edit`/`Redo Edit`, and a taller bottom piano keyboard stays available for mouse auditioning plus laptop-key auditioning with Ableton-style labels: Home maps `A=C3`, `Oct+` maps `A=C4`, and the range clamps before C0. The `LIBRARY` panel is organized as a preset-vault workflow with quick filters for All, Favorites, House, Tech House, Minimal, UKG, Techno, Bass House, Amapiano, Hardgroove, Future Garage, Speed Garage, Deep Tech, Indie Dance, Italo Disco, Balearic House, Acid House, Nu Disco, Afro Tech, Afro Melodic, Progressive House, Hard House, Peak Time Techno, Detroit Techno, Minimal FM, Deep Minimal, Lo-Fi House, French House, Soulful House, Garage House, Microhouse, Raw Techno, Tribal Tech House, Breaks House, Chicago House, Classic House, Funky House, Melodic House, Romanian Minimal, Dub Techno, Electro Breaks, and Factory, smart production crates such as UKG Basslines, Garage Chops, House Chords, Minimal Plucks, Techno Stabs, Dirty Tools, Mono Safe, and Sequenced Grooves, a left crate rail for text search, quick filters, smart crates, sort, and visible user/factory/folder/pack/generated/style coverage, a central production-patch browser with selected-preset navigation and performance-macro previews, one-click rendered preview audition that does not load the patch, a `Warm` action for selected/visible row preview rendering, preview-ready/stale/render-on-audition row status with level badges, explicit Before/Loaded compare and Revert controls for the most recent loaded preset, adaptive preset rows that switch to compact sound/meta plus preview/info columns at narrow widths, text search, sort modes, 1-5 star ratings in the browser action area, a redesigned Save Plan card with destination trail and labeled Name/Folder/Pack/Cue/Notes readiness, editable author/pack/key/BPM metadata, expanded house/techno style folder and pack targets, editable notes and note templates, selected-preset role/trait/cue profile, Tone/Dirt/Motion/Space inspector meters, eight-macro shape preview/search/sort/filtering, recursive category/subfolder saves, and preset filtering for favorites, recents, generated presets, rated presets, user/factory source, smart tags, packs, BPM ranges, and saved categories including House, Tech House, Techno, Minimal, Romanian Minimal, Electro Breaks, and UKG. The `INFO` panel keeps About, workflow, topic explanations, and quick work-area jumps out of the main sound-design panels. The editor opens at 1040x760, keeps 940x710 as the minimum compact host size, and can resize up to 1440x980 in hosts that expose plugin resize handles.

At default and larger editor sizes, the MOD routing section also shows a compact route map above the matrix so active and bypassed movement is visible as source-to-destination paths. At the minimum compact size, the map hides so the editable matrix rows remain usable in Ableton.

User presets are stored at:

```text
~/Library/Application Support/Nate VST/Presets
```

New user preset saves are organized under category folders, for example:

```text
~/Library/Application Support/Nate VST/Presets/UKG/Bass
```

Factory presets can be placed separately at:

```text
~/Library/Application Support/Nate VST/Factory Presets
```

The repo-managed factory pack currently includes 438 starter presets across UKG, speed garage, future garage, breakbeat garage, house, deep-house construction kits, piano-house riffs, jackin organ bass kits, club synth stabs, bass house, Chicago house, classic house, funky house, acid house, indie dance, Italo disco, Balearic house, nu-disco, deep tech, afro tech, Afro House, amapiano, French house, soulful house, garage house, breaks house, tribal tech house, melodic house, afro melodic house, progressive house, hard house, ambient/evolving tools, electro, electro breaks, tech-house, tech-house rubber hook kits, microhouse, Romanian minimal, deep minimal, minimal/FM, raw/hypnotic techno, hardgroove techno, dub techno, dub-house chord throws, dirty house tools, warehouse techno, melodic techno, and techno.

See [PLAN.md](./PLAN.md) for the full roadmap, [docs/HOUSE_VST_COMPETITOR_RESEARCH_2026_06_29.md](./docs/HOUSE_VST_COMPETITOR_RESEARCH_2026_06_29.md) for the current house-focused competitor refresh, [docs/NEXT_FEATURE_RESEARCH.md](./docs/NEXT_FEATURE_RESEARCH.md) for the prioritized add list, [docs/DEEP_RESEARCH_BACKLOG.md](./docs/DEEP_RESEARCH_BACKLOG.md) for the implementation backlog, [docs/PROGRAM_RESEARCH_GAP_LIST.md](./docs/PROGRAM_RESEARCH_GAP_LIST.md) and [docs/COMPETITOR_GAP_ANALYSIS.md](./docs/COMPETITOR_GAP_ANALYSIS.md) for older broad research matrices, [docs/MODULATION_WORKFLOW.md](./docs/MODULATION_WORKFLOW.md) for the first modulation/performance design, [docs/UI_EFFECTS_ROADMAP.md](./docs/UI_EFFECTS_ROADMAP.md) for the next UI grouping and FX rack direction, and [docs/UK_GARAGE_WORKFLOW.md](./docs/UK_GARAGE_WORKFLOW.md) for the UK garage sound direction.

Current house/UKG workflow focus: cleaner oscillator sources, appended procedural Organ and House Piano modes for drawbar-style stabs and piano-house chords, a live/clickable SYNTH house layer rack for seeing and focusing sub/body/character/transient/chop balance, percussive noise ticks/air/vinyl/digital texture, internal wavetable movement, Osc Warp source motion, visible Filter Character/Slope/cutoff/resonance shaping for bass and stab color, visual HOME performance macros, A/B/C/D performance scenes, output oscilloscope/spectrum/stereo/Club Monitor analysis with held spectrum peaks, grouped club-band balance, short-history sub/side/pump/Guard/peak risk, and club low-end guidance, redesigned preset recall/save/library flow with rendered preview cache/playback, visible-row preview warming, and preview level badges, MOD destination inspection with per-route bypass/delete, expressive Mod Wheel/Aftertouch/Pitch Bend/Note matrix sources, and a draggable macro-assignment pad, SEQ chord/voicing/strum plus one-finger Memory, host-sync status, selective groove transforms including `Chord Stab Paint` and `Build 4-Bar Chain`, an adaptive expanded-focus `Auto` / `2 Bar` / `4 Bar` chain length mode for live scene-chain playback and Chain MIDI save/drag export, an assignable per-step lock lane for rhythmic cutoff/drive/warp/pump/delay/reverb/WT movement, saved per-step ratchet fills, a first Cond lane for odd/even/fill phrase variation that reaches playback, scenes, and MIDI save/drag export, a first Slide lane that holds bass/chord notes into the next triggered step for live playback and Ableton MIDI drag-out, and first-pass live scene-chain step/control playback for captured A/B/Fill/Drop scenes, waveform-based sample chopping with click guards, first transient `Detect` slicing into eight custom Slice Keys regions, direct slice-boundary drag editing, slice styles, Classic/Granular/Spectral/Cloud snippet playback, visual slice-state overlays, adaptive expanded chop focus editing, per-slice region/pan/chance/choke/nudge/fade memory, and a first missing-file safety guard that clears stale audio while preserving the saved path for relink/status, section-scoped randomization, and an addable FX rack with send-style throws, first-pass Bass Safe Drive, Guard glue/punch/clip dynamics, and module presets. The current competitor refresh says the biggest remaining house gaps are full manual marker add/remove and beat/time slicing, slice-to-sequencer, time-stretch/full relink, deeper arranger-style scene-chain editing and per-scene automation/rate/follow behavior, Ableton-native audio/clip drag-out beyond the first MIDI drag-out pass, a house drum/kick-bass construction lane, full multiband/oversampled drive, expansion-ready content packs, relationship-aware construction-kit content, and release-grade Ableton validation.

## Local Development

Expected tools:

- Xcode or Xcode Command Line Tools.
- CMake.
- Ninja or Make.
- Ableton Live for host testing.
- pluginval for local VST3 validation.

The initial project scaffold uses CMake FetchContent to download JUCE during configuration.

## Build

Once CMake is installed:

```sh
cmake -S . -B build -G Ninja
cmake --build build --config Debug
ctest --test-dir build --output-on-failure
```

The current test suite includes `EditorLayoutAudit`, which opens the real plugin editor and checks all panels, every Random Lab page, FX detail views, the expanded HOME/MOD macro focus overlay, expanded SAMPLE chop focus overlay, expanded SYNTH source-layer focus overlay, and expanded SEQ pattern focus overlay at minimum, minimum-tall, default, default-tall, wide-min-height, wide, and maximum editor sizes for visible controls with empty or out-of-bounds layouts plus explicit SYNTH house-layer-rack visibility/overlap/card-readability checks, adaptive tall focus-overlay growth checks, expanded SAMPLE waveform height checks, expanded SEQ focus-grid height checks, responsive bottom-keyboard key coverage, Ableton-style laptop octave labels/clamping, keyboard-control compression checks, compact Library browser row readability, compact MOD source-meter/route-amount readability, and compact and expanded SEQ grid row/cell/readability checks, `SampleWaveformDisplayAudit`, which verifies direct slice-boundary dragging and slice-lane selection remain distinct, `SampleRecorderAudit`, which verifies SAMPLE recorder capture, commit-to-sampler behavior, waveform overview generation, and audible playback from the committed snippet, `WavetableDisplayAudit`, which verifies the SYNTH wavetable editor keeps readable frame-rail/plot/partial/spectrum zones, renders nonblank visual feedback, and still emits direct point and partial-bar edits, `ThemeAudit`, which verifies critical text/accent contrast across the current Dark Club palette and planned Analyzer/Warm Studio theme palettes, `PresetSaveAudit`, which verifies nested category saves, normalized preset metadata, macro-value preview scanning, same-name preset preservation across separate category folders, user-preset existence checks, and load-by-name behavior, `FactoryPresetLibraryAudit`, which verifies the 438-preset factory style pack scans and load-checks repo-managed presets with metadata/notes, `PresetPreviewAudit`, which verifies rendered preview batch creation, cached-skip behavior, forced regeneration, preview level metadata, non-destructive audition playback, and recents preservation for representative house roles, `FactoryPresetRenderAudit`, which renders every repo-managed factory preset through the real preset loader and synth/sample/FX path for finite, non-silent, peak-contained output, `RandomCandidateAudit`, which verifies generated-candidate capture, useful-only validation, auto-retry/fallback reporting, compare/diff summaries, non-destructive cue preview, save-from-slot, favorite/rating persistence, recall, and snapshot promotion, `GlobalEditHistoryAudit`, which verifies first-pass global undo/redo restores synth parameters, modulation routes, sequencer step data including ratchet/condition/slide values, and performance snapshot state, `PumpTelemetryAudit`, which verifies Pump reports live ducking telemetry and clears it when disabled, `GuardTelemetryAudit`, which verifies hot patches report Guard protection telemetry and disabled Guard clears the meter state, `GuardDynamicsAudit`, which verifies Guard punch, glue compression, and clip-ceiling behavior, `DriveBassSafetyAudit`, which verifies Bass Safe Drive keeps low-band energy closer to the clean reference while still adding high-band saturation, `SequencerPatternSceneAudit`, which verifies pattern scene capture/recall, save/restore, condition-aware scene-chain export, live-chain enabled-state restore, forced 2-bar/4-bar chain-length state/export, and the `Build 4-Bar Chain` transform's generated scenes, undo restore, and four-bar export anchors, `SequencerHousePatternAudit`, which verifies house chord colors, chord-stab paint, chord/bassline pattern presets, appended groove-mode compatibility, and the house/UKG/tech-house/minimal/techno groove templates, `SequencerRatchetAudit`, which verifies per-step ratchet/condition/slide clamp/state/playback, first-pass live scene-chain note alternation plus cached scene root/chord-color playback, two-scene Fill parity, exported MIDI note timing, and slide overlap in live/exported MIDI, `NoiseSourceAudit`, which verifies noise-source choices, Tick decay, and sustained white/digital noise rendering, `SourceCharacterAudit`, which verifies waveform choice ordering and finite/audible Organ and House Piano source rendering, `ModulationSourceParityAudit`, which verifies sample and FX `Mod Env 1`/`Velocity` routes are audible/effective for producer-facing destinations, `SamplerChopAudit`, which verifies sample-load slice reset, transient detection into ordered custom Slice Keys regions, custom Slice Keys playback, probability suppression, nudge/fade playback changes, missing-file restore clearing stale audio while preserving relink path state, and UKG chop setup, `EffectsSendAudit`, which verifies send delay tails, one-shot tail kill behavior, and nonzero processor tail reporting, `SpectrumSnapshotAudit`, which verifies final-output spectrum snapshots start silent, capture rendered output, stay finite, and reset on prepare, and `StereoFieldTelemetryAudit`, which verifies stereo field telemetry resets cleanly and remains finite/in range after rendered output.

The generated VST3 can be copied to:

```text
~/Library/Audio/Plug-Ins/VST3
```

See [CHANGELOG.md](./CHANGELOG.md) for detailed implementation notes by iteration.
